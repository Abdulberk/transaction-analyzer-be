// src/types/pattern.types.ts
import {
  PatternType as PrismaPatternType,
  Frequency as PrismaFrequency,
} from '@prisma/client';

export { PrismaPatternType as PatternType, PrismaFrequency as Frequency };
